import { NumberCommaPipe } from './number-comma.pipe';

describe('NumberCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
